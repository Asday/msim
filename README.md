# Developing

```shell
pip install -r requirements-dev.txt
./watch
```

```shell
docker-compose up
```

# Running tests

`tox --parallel all`.

# Running the project

```shell
docker-compose up
```

```shell
./manage migrate
```
